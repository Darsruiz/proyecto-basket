import { AumentarDirective } from './aumentar.directive';

describe('AumentarDirective', () => {
  it('should create an instance', () => {
    const directive = new AumentarDirective();
    expect(directive).toBeTruthy();
  });
});
