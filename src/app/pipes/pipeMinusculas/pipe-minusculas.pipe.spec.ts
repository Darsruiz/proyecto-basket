import { PipeMinusculasPipe } from './pipe-minusculas.pipe';

describe('PipeMinusculasPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeMinusculasPipe();
    expect(pipe).toBeTruthy();
  });
});
